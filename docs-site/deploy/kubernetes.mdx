---
title: "Kubernetes deployment"
description: "Choose standalone engine workloads or Cloud-managed engines, then follow the matching Kubernetes runbook."
icon: "dharmachakra"
---

VentStream has two Kubernetes operating modes. Both run the same public engine
image (`ghcr.io/ventstream/ventstream`); the mode is decided at runtime by the
presence of a deployment [agent key](/fleet/enrollment). Choose the mode before
creating workloads because configuration ownership differs — an agent key
alongside local source/sink configuration is a hard startup error.

| Mode | Engine process | Configuration owner | Operations | Control plane |
| --- | --- | --- | --- | --- |
| Standalone | The same `ventstream` process reading a local `ventstream.yaml`; no agent key | ConfigMap and Secret | Helm/Kubernetes restart and rollout | None |
| Cloud-managed | The same `ventstream` process started with an agent key; configuration comes from VentStream Cloud | Immutable Cloud revision plus workload-local Secret | Dashboard and `ventstreamctl` | VentStream Cloud |

## Standalone

Use standalone mode when each workload should be entirely self-contained and
restart-based lifecycle control is sufficient.

- [Deploy a standalone engine](/deploy/kubernetes-standalone)
- [Canonical engine configuration](/reference/engine-env#canonical-engine-config)

The standalone guide provides a current canonical-config StatefulSet for CDC.
Use the `ventstream-gateway` chart for replicated native WebSocket and GraphQL
subscription roles.

## Cloud-managed

Use managed mode when operators need organization-scoped inventory, immutable
configuration, agent health, audit events, and CLI pause, resume, drain,
reconcile, or rebootstrap operations.

1. Create or join a [VentStream Cloud](https://ventstream.dev/signup)
   organization.
2. Mint a deployment [agent key](/fleet/enrollment) and
   [deploy a managed engine](/deploy/kubernetes-managed-engine) into your
   Kubernetes cluster.
3. [Administer it with ventstreamctl](/fleet/cli) or the dashboard.

VentStream Cloud never enters the CDC or realtime data path. Managed engines
connect outbound to the Cloud agent gateway over mTLS; source records, sink
writes, broker events, WebSocket connections, and GraphQL subscriptions remain
inside the workload environment.

## Common production requirements

- Pin container images by digest and promote the same digest between environments.
- Keep connector credentials and private keys in a managed secret provider.
- Persist source cursors, join state, managed identity, and applied configuration.
- Give every CDC pipeline its own replication slot or cursor and exactly one
  active engine instance.
- Run `helm lint` and `helm template` against production values in CI.
- Never delete managed-agent PVCs during routine upgrades.
