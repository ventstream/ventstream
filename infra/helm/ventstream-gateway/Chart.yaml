apiVersion: v2
name: ventstream-gateway
description: VentStream real-time gateway — the stateless WebSocket (ws) and/or GraphQL (graphql) fan-out roles. Horizontally scalable; one Deployment, not per-tenant.
type: application
version: 0.1.21
appVersion: "0.1.21"
keywords:
  - ventstream
  - websocket
  - graphql
  - subscriptions
home: https://github.com/ventstream/ventstream
