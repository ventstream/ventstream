# Copy to terraform.tfvars and adjust. All have sensible test defaults.
region = "eu-central-1"

# Smaller/cheaper for a quick test:
# db_instance_class         = "db.t3.medium"
# opensearch_instance_type  = "t3.small.search"
# node_instance_type        = "m5.large"
# node_desired_size         = 3

# Per-pod WS connection cap (size to node/pod memory; PR #7).
# ws_max_conns = 5000
